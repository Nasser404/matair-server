services:
  matair-server:
    build: 
      context: .
      dockerfile: Dockerfile
    container_name: matair_backend
    ports:
      # Maps your host machine port to the container port defined in your Dockerfile
      - "29920:29920"
    restart: unless-stopped